// Modality feature namespace. The active modality implementation still lives
// in app.js while the shared configuration/auth split lands first.
(function () {
  window.COSModality = {
    enabled: () => true
  };
})();
