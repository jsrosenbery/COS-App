// Utilization feature namespace. The active utilization implementation still
// lives in app.js while feature modules are introduced safely.
(function () {
  window.COSUtilization = {
    enabled: () => true
  };
})();
